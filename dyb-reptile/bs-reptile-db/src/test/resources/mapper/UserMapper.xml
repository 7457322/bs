<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="bs.reptile.db.mapper.UserMapper">
    <select id="findAll" resultType="bs.reptile.db.entity.User">
        SELECT * FROM t_user
    </select>

    <select id="findAllPage" resultType="bs.reptile.db.entity.User">
        SELECT * FROM t_user
    </select>
</mapper>
